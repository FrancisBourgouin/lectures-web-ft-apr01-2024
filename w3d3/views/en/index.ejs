<%- include('partials/_head.ejs') %>
<section>
	<h1>
		<span>Super secret thief page !</span>
	</h1>
		<h2>
			<% if(thief){ %>
			Welcome <%= thief.name %>!
			<% } %>
		</h2>
</section>
<section>
	<main>
		<div class="door">
		</div>
		<% if(thief){ %>
		<img src="<%= thief.imagePath %>" alt="">
		<% } %>
		</main>
		</section>
	<section>
			<% if(!thief){ %>
				<a href="/login">Log in into the vault</a>
				<a href="/register">Create a vault</a>
			<% } %>
			<% if(thief){ %>
				<a href="/logout">Close the vault</a>
			<% } %>
	</section>
	</body>

	</html>
